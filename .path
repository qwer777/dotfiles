##This file sets the PATH variable
OLD_PATH="$PATH"
PATH="$HOME/bin"
PATH="$PATH:$SHAREDBINDIR"
PATH="$PATH:$HOME/go/bin"
PATH="$PATH:/usr/local/go/bin"
PATH="$PATH:/usr/local/sbin"
PATH="$PATH:/usr/local/bin"
PATH="$PATH:/usr/sbin"
PATH="$PATH:/usr/bin"
PATH="$PATH:/sbin"
PATH="$PATH:/bin"
PATH="$PATH:."
PATH="$PATH:$OLD_PATH"
PATH=`awk -F: '{for(i=1;i<=NF;i++){if(!($i in a)){a[$i];printf s$i;s=":"}}}'<<<$PATH`
export PATH="$PATH"
